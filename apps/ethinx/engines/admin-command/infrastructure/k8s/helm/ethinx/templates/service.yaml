apiVersion: v1
kind: Service
metadata:
  name: {{ include "ethinx.fullname" . }}
  labels:
    {{- include "ethinx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ethinx.selectorLabels" . | nindent 4 }}
